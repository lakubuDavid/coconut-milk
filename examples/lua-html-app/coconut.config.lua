return {
  window_width = 720,
  window_height = 720,
  resizable = false,
  initial_view = "home",
  view_root = "views",
  asset_root = "assets",
  command_root = "commands",
  views = {},
  debug = {
    enabled=true,
    logLevel="debug"
  }
}
