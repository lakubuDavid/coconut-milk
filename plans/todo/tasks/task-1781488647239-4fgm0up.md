# **User-defined helper methods** (like `coconut.fs.readText()`, `coconut.dialog.open()`) — these are convenience wrappers that internally call the builtin command. Should I add their signatures to `BuiltinCommandParams/Returns` too? (Technically you call them via `coconut.fs.readText()`, not `coconut.call("fsReadText", ...)`, but typing the raw name helps for `coconut.call()` usage.)

## Description
**User-defined helper methods** (like `coconut.fs.readText()`, `coconut.dialog.open()`) — these are convenience wrappers that internally call the builtin command. Should I add their signatures to `BuiltinCommandParams/Returns` too? (Technically you call them via `coconut.fs.readText()`, not `coconut.call("fsReadText", ...)`, but typing the raw name helps for `coconut.call()` usage.)

## Status
completed

## Dependencies
None

## Parent
None

## Created
2026-06-15T01:57:27.239Z

## Updated
2026-06-15T12:09:43.782Z

---

*This task is part of the project plan managed by /plan command*
