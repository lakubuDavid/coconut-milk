# Fix 11 failing tests

All 11 pre-existing failures fixed:

- 4× config JSON view validation → now returns InvalidConfig for bad/missing kind/src
- 1× config JSON manifests target_archs → added JSON array parsing
- 2× config JSON frameless/transparent type validation → type checks before copy
- 1× fs::create(nullptr) → null guard returns InvalidConfig
- 1× fs::resolve dot handling → "/base/." preserved instead of normalized away
- 1× fs::listDir dotfiles → "." and ".." entries prepended
- 1× bridge::create(nullptr) → null guard returns InvalidConfig

## Status: completed

Created: 2026-06-13
Updated: 2026-06-13
